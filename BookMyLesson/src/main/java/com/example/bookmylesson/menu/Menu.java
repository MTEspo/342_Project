package com.example.bookmylesson.menu;

import java.util.Scanner;

/* This is just a temporary user interface! In future, this will be removed and a 
 * controller will be created to expose the functionality of the system as API endpoints.
 */

public interface Menu {
	void menu(Scanner scanner);
	void displayMenu();
}
